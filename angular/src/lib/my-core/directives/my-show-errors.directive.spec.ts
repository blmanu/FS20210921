import { MyShowErrorsDirective } from './my-show-errors.directive';

describe('MyShowErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MyShowErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
